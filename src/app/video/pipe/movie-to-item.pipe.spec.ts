import { MovieToItemPipe } from './movie-to-item.pipe';

describe('MovieToItemPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieToItemPipe();
    expect(pipe).toBeTruthy();
  });
});
